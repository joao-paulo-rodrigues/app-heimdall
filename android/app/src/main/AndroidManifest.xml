<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />
    
    <!-- Permissão customizada para compartilhar dados com PanicButton -->
    <permission
        android:name="com.heimdall.device.READ_SHARED_DATA"
        android:protectionLevel="signature" />
    
    <!-- Visibilidade do PanicButton (Android 11+) -->
    <queries>
        <package android:name="com.uebrasil.panicbuttonapp"/>
        <intent>
            <action android:name="com.uebrasil.panicbuttonapp.PANIC_TRIGGERED"/>
        </intent>
        <intent>
            <action android:name="com.uebrasil.panicbuttonapp.STATUS_UPDATE"/>
        </intent>
        <intent>
            <action android:name="com.uebrasil.panicbuttonapp.SENSOR_DATA"/>
        </intent>
        <intent>
            <action android:name="com.heimdall.device.COMMAND"/>
        </intent>
        <intent>
            <action android:name="com.heimdall.device.CONFIG"/>
        </intent>
    </queries>

    <application
        android:name=".HeimdallApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Heimdall"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Heimdall"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Heimdall.NoActionBar"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.HeimdallDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>
        
        <!-- Receiver para eventos do PanicButton -->
        <receiver
            android:name=".panicbutton.PanicButtonEventReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.uebrasil.panicbuttonapp.PANIC_TRIGGERED" />
                <action android:name="com.uebrasil.panicbuttonapp.STATUS_UPDATE" />
                <action android:name="com.uebrasil.panicbuttonapp.SENSOR_DATA" />
            </intent-filter>
        </receiver>

    </application>

</manifest>

